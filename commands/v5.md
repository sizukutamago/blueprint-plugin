---
name: v5
description: Run the full v5 workflow pipeline with Review Swarm quality gates. Automates /spec → /test-from-contract → implementation → /generate-docs.
---

# v5 Orchestrator Command

v5 パイプラインをワンコマンドで自動実行する。

## 使用方法

```
/v5              # パイプライン実行（Smart Skip 適用）
/v5 --resume     # 中断点から再開（任意ステージ対応）
/v5 --force      # 全ステージ強制実行（Smart Skip 無視）
```

## パイプライン

```
Stage 1: /spec → Contract Review Gate
Stage 2: /test-from-contract → Test Review Gate
Stage 3: Implementation Pause（ユーザーが実装）
Stage 4: /generate-docs → Doc Review Gate
```

各ステージ後に **Review Swarm**（3 並列エージェント）が品質レビューを実行。
Gate 判定: P0=0 かつ P1≤1 → PASS、それ以外 → REVISE（最大 2 サイクル）。

## Smart Skip

既存の成果物を検出すると、該当ステージのスキップをユーザーに提案する。
スキップ時もレビューゲートは実行する。`--force` で全ステージ強制実行。

## 出力先

- `.knowledge/contracts/` — Contract YAML（Stage 1）
- `tests/contracts/` — TDD テスト（Stage 2）
- `docs/` — 設計書（Stage 4）
- `.knowledge/pipeline-state.yaml` — パイプライン状態

## 前提

- Git リポジトリ内であること
- テストフレームワークは自動検出（未検出時はユーザーに確認）

## 次のステップ

1. Stage 3 で一時停止後、Level 2 テストを実装して GREEN にする
2. `/v5 --resume` で Stage 4 を再開
3. 完了後、P2 要対応リストを手動で修正

## 関連

- `/spec` — Contract YAML 生成（Stage 1 で実行）
- `/test-from-contract` — TDD テスト生成（Stage 2 で実行）
- `/generate-docs` — 設計書生成（Stage 4 で実行）
- v5-orchestrator スキル（`core/v5/orchestrator.md` でワークフロー定義）
